#version 460 core

in vec3 VertexColour;
out vec4 FragColor;

void main() {
	//Substitute the rgb values as VertexColour as it has the rgb values inside of it

	FragColor = vec4( VertexColour, 1.0f );
}